[project]
name = "mas"
version = "0.1.0"
requires-python = ">=3.12,<3.15"
dependencies = ["openhands-sdk (>=1.17.0,<2.0.0)", "openhands-tools (>=1.17.0,<2.0.0)", "pydantic (>=2.13.1,<3.0.0)"]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false